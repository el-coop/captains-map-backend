import faker from 'faker';
import {{model}} from '../../App/models/{{model}}.js';
import BaseFactory from './BaseFactory.js';

class {{name}} extends BaseFactory {
	model() {
		return {{model}};
	}

	define() {
		return {
		}
	}
};

export default new {{name}}();